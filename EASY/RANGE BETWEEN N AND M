def print_numbers(M, N, K):
    for num in range(M, N+1, K+1):
        print(num, end=" ")
input_values = list(map(int, input("Enter M, N, K separated by spaces: ").split()))
M, N, K = input_values[0], input_values[1], input_values[2]
print_numbers(M, N, K)
